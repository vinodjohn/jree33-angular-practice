export class Dog {
    name: string;
    heightInCm: number;
    isAlive: boolean;

    constructor(name: string, heightInCm: number, isAlive: boolean) {
        this.name = name;
        this.heightInCm = heightInCm;
        this.isAlive = isAlive;
    }
}
